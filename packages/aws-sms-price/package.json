{
  "name": "@tsukiy0/aws-sms-price",
  "version": "0.0.0",
  "main": "dist/index.js",
  "typings": "dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "lint": "eslint \"./src/**/*.ts\"",
    "typecheck": "tsc --noEmit",
    "test": "jest",
    "prebuild": "rm -rf dist",
    "build": "tsc --rootDir src --outDir dist"
  },
  "dependencies": {},
  "devDependencies": {
    "config": "*",
    "cross-fetch": "^3.1.5",
    "tsconfig": "*",
    "typescript": "^4.5.3"
  }
}
